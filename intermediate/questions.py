"""
python function: takes a numbers of input request perform and action and return an output

local variable: are variable that can only be use within it block or function
global variable: are variable that are access anywhere in the code block

recursion: is the  function that call itself 

list comprehension means that it create a list using one line of code using for loop
example: new_list = [expressoon for item in iterable]

module is a python file that cointian variable fucntion classes
try and execpt is use to catch erroes

try:
    pass
except Exception as e:
    str(e)
finally:
"""